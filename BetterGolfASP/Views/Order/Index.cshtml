@using BetterGolfASP.Models.Products
@model IEnumerable<GolfClub>

<h2>@ViewBag.Category</h2>

<@* div class="row g-4">
    @foreach (var club in Model)
    {
        <div class="col-sm-6 col-md-4 col-lg-3">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center">
                    <h5 class="card-title">@club.Name</h5>
                    <p class="text-success fw-bold">@club.Price.ToString("C")</p>
                    <a href="@Url.Action("Details", "Products", new { id = club.GolfClubID })" class="btn btn-sm btn-outline-success">View</a>
                </div>
            </div>
        </div>
    }
</div> *@
